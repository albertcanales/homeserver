---
- name: Backup config volume
  include_role:
    name: my_docker
    tasks_from: backup_volumes
  vars:
    volume_names:
      - mealie-data
    container_name: mealie
    image_name: ghcr.io/mealie-recipes/mealie:nightly