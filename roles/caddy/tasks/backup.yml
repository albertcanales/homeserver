---
- name: Backup config volume
  include_role:
    name: my_docker
    tasks_from: backup_volumes
  vars:
    volume_names:
      - caddy-config
      - caddy-data
    container_name: caddy
    image_name: caddy-duckdns
